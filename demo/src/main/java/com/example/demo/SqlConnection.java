package com.example.demo;
import java.sql.*;
public class SqlConnection {
    public static void main (String[] args) {
        //steps to the connection between java and database
        //jdbc-java database connectivity
        //there are seven steps in the connection
        //* import java sql packages
        //import java.sql.*;
        //* load and register the driver
        //Class.forName("com.sql.jdbc.Driver)
        //*getConnection
        //getConnection("URL","username","password");
        //Connection con=Driver.getConnection("URL","username","password");
        //create the statement object
        //Statement st=con.createStatement();
        //ResultSet rs=st.executeQuery("select*from students");
        //steps to create the connection betwenn java and database
        //we have to import the packages
        //load and register the driver
        //get connection
        //create statēment object
        //query execution
        //results
        //close connection

    }
}
